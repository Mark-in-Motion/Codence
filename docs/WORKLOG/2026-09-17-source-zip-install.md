# 2026-09-17 — Source ZIP installation instructions

- Clarified that v0.1.0 is source-only, with no downloadable `Codence.app` or DMG.
- Linked the v0.1.0 release directly from the README install steps and described **Assets → Source code (zip)**, extraction, opening Terminal in the source folder, and running `./scripts/install-local.sh`.
- Kept `git clone` as an alternative and retained the full Xcode and Codex CLI requirements.
- Confirmed the v0.1.0 source archive includes `scripts/install-local.sh` with executable mode.
- Documentation-only change; the application and release status are unchanged.
